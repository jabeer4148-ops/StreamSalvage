export function ReferenceExplainer() {
  return (
    <section className="rounded border border-slate-200 bg-slate-50 p-4 text-sm text-slate-700">
      A short healthy recording from the same OBS settings gives FFmpeg the codec and container details it needs to rebuild a damaged MP4.
    </section>
  );
}
